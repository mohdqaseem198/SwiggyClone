import react from "react";

const LazyComponent = () => {
    return(
        <div>
            <h2 className="font-bold text-2xl text-center m-10">Hi, I'm lazy loading Components</h2>
        </div>
    )
};

export default LazyComponent ;